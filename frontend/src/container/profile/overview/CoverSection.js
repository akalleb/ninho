import React from 'react';
import propTypes from 'prop-types';

function CoverSection() {
  // Usuário solicitou remover a imagem de capa.
  // Retornando null para não renderizar nada ou apenas um container vazio se necessário para layout.
  return null;
}

CoverSection.propTypes = {
  match: propTypes.object,
};

export default CoverSection;
